@page "{id}"
@model ShopAppUI.Pages.Items.ListModel
@{
    ViewData["Title"] = "Shop Items";
}
<div class="text-center">
    <h4>@ViewData["Title"]</h4>
    <h4>@Model.categoryViewModel.Name </h4>
    <partial name="_ShopItemPartial" model="Model.Items" />
</div>
